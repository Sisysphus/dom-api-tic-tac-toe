const currentPlayerSymbol = 'X';
const squareValues = `[''], [''], [''], [''], [''], [''], [''], [''], ['']`;

window.addEventListener('DOMContentLoaded', (event) => {
    
const ticTacHandler = document.getElementById('tic-tac-toe-board')
ticTacHandler.addEventListener('click', (event))








})

